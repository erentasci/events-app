import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
    padding: 16,
  },
  input: {
    backgroundColor: "#fff",
    borderRadius: 8,
    paddingLeft: 40,
    paddingRight: 12,
    paddingVertical: 12,
    fontSize: 14,
  },
  searchIcon: {
    position: "absolute",
    left: 24,
    top: 24,
    zIndex: 1,
  },
});

export default styles;
